export type TRGB = `${ "rgb"|"RGB" }(${ number },${ number },${ number })`;
export type TRGBA = `${ "rgba" | "RGBA" }(${ number },${ number },${ number },${ number })`;
export type THex = `#${ string }`;
